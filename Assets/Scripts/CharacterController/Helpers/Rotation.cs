using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Rotation : MonoBehaviour
{
    /*
     * Use cases:
     * Dialogue
     *  Track something permanently, until another LookAt function is called and overrides it
     *  
     *  LookAt until reach position (angle-based) + hold there for some seconds (time-based)
     *      (so its not running all the time)
     *  
     *  
     *  Not sure if needed:
     *      LookAt until some kind of condition is true?
     */
}
